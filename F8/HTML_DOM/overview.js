
// HTML DOM
 
// có 3 thành phần

/**
 * 1. Element: là các thẻ HTML. ID, class, tag, css selector, html collection
 * 2. Attribute: là các thuộc tính của thẻ HTML
 * 3. Text: là nội dung của thẻ HTML
 */

// Node: là tất cả các thành phần của HTML DOM (Element, Attribute, Text)


// browser: html -> DOM -> DOM API (web api)